/**
 * Projects for seat map. Re-exports from departures with optional project-level links.
 */

export type { Project, FlightStatus } from "./departures";
export { projects, getProjectBySlug } from "./departures";
